package com.example.drilldownpattern.emuns

/**
 *  usage: enum for defining the types of Item in Recycler View
 *  @author Nikhil Gupta
 */
enum class MetricViewType(val type: Int) {
    HEADER(1),
    METRIC_ITEM(2)
}